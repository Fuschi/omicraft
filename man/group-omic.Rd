% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-grouping.R
\name{group-omic}
\alias{group-omic}
\alias{get_group_omic}
\alias{get_group_omic,omic-method}
\alias{get_group_omic,omics-method}
\alias{group_omic}
\alias{group_omic,omic-method}
\alias{group_omic,omics-method}
\alias{ungroup_omic}
\alias{ungroup_omic,omic-method}
\alias{ungroup_omic,omics-method}
\alias{is_omic_grouped}
\title{Grouping helpers for \code{omic} / \code{omics}}
\usage{
get_group_omic(object)

\S4method{get_group_omic}{omic}(object)

\S4method{get_group_omic}{omics}(object)

group_omic(object, ..., .add = FALSE)

\S4method{group_omic}{omic}(object, ..., .add = FALSE)

\S4method{group_omic}{omics}(object, ..., .add = FALSE)

ungroup_omic(object, ...)

\S4method{ungroup_omic}{omic}(object, ...)

\S4method{ungroup_omic}{omics}(object, ...)

is_omic_grouped(object)
}
\arguments{
\item{object}{An \code{omic} or \code{omics} object.}

\item{...}{One or more variable names (bare or strings). May also include a
single \code{NULL} to drop all groups. In \code{ungroup_omic()}, names in \code{...} are
removed from the current grouping vector.}

\item{.add}{Logical; if \code{TRUE}, add to existing groups; if \code{FALSE}, replace.}
}
\value{
\itemize{
\item \code{group_omic()} returns the modified object.
\item \code{ungroup_omic()} returns the modified object.
\item \code{get_group_omic()} returns a character vector (possibly length-0).
\item \code{is_omic_grouped} returns logical.
}
}
\description{
Set or get grouping variables on \code{omic} / \code{omics} objects, with semantics
similar to \code{dplyr::group_by()} / \code{dplyr::ungroup()}.
}
\details{
The grouping state is stored as an attribute named "omic_groups" on the
object (for both \code{omic} and \code{omics}).

Semantics:
\itemize{
\item \code{group_omic(x, a, b)} replaces the current groups with \code{c("a","b")}.
\item \code{group_omic(x, a, .add = TRUE)} adds \code{"a"} to the existing groups.
\item \code{group_omic(x)} with no grouping vars returns \code{x} unchanged (keep current).
\item \code{group_omic(x, NULL)} clears the groups (like \code{group_by(NULL)}).
\item \code{ungroup_omic(x)} clears all groups.
\item \code{ungroup_omic(x, a)} removes \code{"a"} from the current groups (if present).
\item \code{is_omic_grouped(x)} returns TRUE if grouping vars are stored.
}

Validation:
For \code{omic}, valid fields come from \code{meta_taxa_vars(object)}.
For \code{omics}, valid fields come from \code{meta_taxa_vars(object, .fmt = "unique")}.
}
