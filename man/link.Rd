% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-getters.R
\name{link}
\alias{link}
\alias{link,omic-method}
\alias{link,omics-method}
\title{Retrieve network links (edges)}
\usage{
link(object, selected = FALSE, .fmt = c("list", "tbl"))

\S4method{link}{omic}(object, selected = FALSE, .fmt = c("list", "tbl"))

\S4method{link}{omics}(object, selected = FALSE, .fmt = c("list", "tbl"))
}
\arguments{
\item{object}{An \code{omic} or \code{omics} object.}

\item{selected}{Logical scalar. If \code{TRUE}, use the selected network (if your
class distinguishes selected vs full network). For \code{omic} the default is
\code{FALSE}. For \code{omics} the default is \code{TRUE}.}

\item{.fmt}{For \code{omics} only, output format: one of \code{"list"} (named list of
tibbles) or \code{"tbl"} (single tibble with column \code{omic}). Ignored for \code{omic}.}
}
\value{
\itemize{
\item For an \code{omic}: a tibble/data frame of edges.
\item For an \code{omics} with \code{.fmt = "list"}: a \strong{named list} of edge tibbles.
\item For an \code{omics} with \code{.fmt = "tbl"}: a single tibble with an \code{omic} column.
}
}
\description{
\code{link()} returns the edge table of the network stored in an \code{omic} or, for an
\code{omics}, returns one edge table per contained object (as a named list) or a
single stacked tibble with an \code{omic} column.
}
\section{Edge table schema}{

Matches \code{igraph::as_data_frame(g, what = "edges")}, i.e. at minimum:
\itemize{
\item \code{from}, \code{to} (character vertex ids),
\item any additional edge attributes (e.g., \code{weight}, \code{link_id}, ...).
}
}

